# Current Versus Remaining Scope Ambiguity

The plan references work that is already implemented and then says to finish the
remaining pieces, but it never identifies the current scope or the remaining scope by
file, behavior, or validation command.

Expected review finding: the distinction between already implemented behavior and
remaining work is ambiguous enough to cause duplicated or skipped implementation.

# Implementation Agent Must Invent Behavior

The plan says to add saved defaults and choose the best fallback at implementation time.
It does not specify the default behavior when the stored value is missing, invalid, or
conflicts with a newly selected mode.

Expected review finding: the implementation agent would have to invent behavior because
the plan leaves the default behavior unspecified.

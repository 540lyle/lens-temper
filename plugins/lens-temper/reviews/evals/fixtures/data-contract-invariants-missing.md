# Data Contract Invariants Missing

The plan introduces a data contract with required fields but does not list invariants,
invalid states, migration behavior, or compatibility expectations for older records.

Expected review finding: data contract invariant coverage is missing, and invalid input
handling is unspecified.
